---
title: Custom id
description: Supports ability to set a custom id for the toast.
---

Supports ability to set a custom id for the toast.

## Example

```ts
import { toast } from "@toastup/react";

const id = toast.add({ id: "99" });
console.log(id); // 99
```

:::info
`id` option is available only for the add function.
:::

import { Blob1 } from "@site/src/components/Blobs/Blob1";

<Blob1 />
